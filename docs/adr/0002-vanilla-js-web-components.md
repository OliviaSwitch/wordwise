# Vanilla JS + Web Components

The UI is built with plain JavaScript and Web Components (custom elements, shadow DOM) with no framework. The application surface is small — a shelf view, a scrolling reader, and a settings panel — and does not justify the bundle size or maintenance cost of a framework. JSZip is the only runtime dependency beyond the Gloss Pack data.
